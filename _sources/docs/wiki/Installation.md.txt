## Pre-requisites

You need Python >=3.10 on your machine to install `python-template`.

## Install with `pip`

```bash
pip install python-template
```

## Install with `conda`

```bash
conda install python-template --channel conda-forge
```

## Source installation

For other platforms and for development installations, [build `python-template` from source](Build-from-Source).
